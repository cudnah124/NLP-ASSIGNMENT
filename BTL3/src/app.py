"""
Main UI application (e.g., Streamlit) to interact with the RAG pipeline.
"""

# import streamlit as st
# from rag_pipeline import ask_contract

# st.title("Contract QA System")
# query = st.text_input("Ask a question about the contract:")
# if query:
#     answer = ask_contract(query)
#     st.write(answer)
